<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
    <style>
        /* Grille pour les cartes de statut principales (en bas) */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
        }
        .stat-card {
            background-color: var(--card-bg-color); 
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
        }
        .stat-card:hover {
            transform: translateY(-5px);
        }
        .stat-card h3 {
            margin-top: 0;
            color: var(--text-color);
            font-size: 1em;
        }
        .stat-card p {
            font-size: 2.5em;
            font-weight: bold;
            margin: 0;
        }
        
        /* Couleurs pour les statuts d'idées */
        .status-Draft { color: #f1c40f; }
        .status-InProgress { color: #3498db; }
        .status-Published { color: #2ecc71; }
        .status-Archived { color: #95a5a6; }
        .status-default { color: var(--text-color); } 

        /* NOUVEAU STYLE POUR LE HAUT DU DASHBOARD */
        .top-dashboard-layout {
            display: flex;
            gap: 20px;
            margin-bottom: 30px;
            flex-wrap: wrap; 
        }
        /* Style pour le graphique (plus petit) */
        .chart-container {
            width: 100%;
            max-width: 350px; 
            height: 350px; 
            margin: 0;
            background-color: var(--card-bg-color);
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            flex-shrink: 0;
        }
        
        /* CONTENEUR DROIT: VOS STATS (Nouveau) */
        .right-kpi-container {
            flex-grow: 1; 
            background-color: var(--card-bg-color);
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            /* Centrage retiré pour un affichage en bloc plus traditionnel */
        }
        .right-kpi-container h3 {
            color: var(--text-color);
            margin-top: 0;
            padding-bottom: 5px;
            border-bottom: 1px solid var(--border-color);
        }

        /* Grille pour les 3 KPIs de la chaîne (Vues/Visionnage/Abonnés) */
        .stats-grid-small {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 15px;
            margin-bottom: 25px;
        }
        .stat-card-small {
            background-color: var(--main-bg-color);
            padding: 10px;
            border-radius: 6px;
            text-align: center;
        }
        .stat-card-small h4 {
            font-size: 0.9em;
            color: var(--secondary-color);
            margin-top: 0;
            margin-bottom: 5px;
        }
        .stat-value {
            font-size: 1.5em;
            font-weight: bold;
            color: var(--primary-color);
            margin: 0;
        }

        /* Styles pour les barres de progression de monétisation */
        .progress-container {
            margin-bottom: 15px;
        }
        .progress-container p {
            font-size: 0.9em;
            font-weight: bold;
            margin-bottom: 5px;
        }
        .progress-bar-bg {
            background-color: var(--main-bg-color);
            border-radius: 10px;
            height: 10px;
            overflow: hidden;
        }
        .progress-bar {
            height: 100%;
            background-color: #2ecc71; /* Vert pour la progression */
            transition: width 0.5s ease-in-out;
        }


        /* Style pour le bouton Mode Sombre */
        #dark-mode-toggle {
            background: none;
            border: none;
            cursor: pointer;
            font-size: 1.5em;
            color: var(--text-color);
            margin-left: 15px;
            transition: color 0.3s;
        }
        #dark-mode-toggle:hover {
            color: var(--primary-color);
        }
    </style>
</head>
<body class="light-mode"> 
    
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
        <p style="font-size: 1.1em; font-weight: bold; color: var(--text-color);">
            Bonjour, <%= username %> !
        </p>
        
        <div style="display: flex; align-items: center;">
            <a href="/logout" class="btn btn-delete">
                Se déconnecter
            </a>
            <button id="dark-mode-toggle" aria-label="Activer le mode sombre">🌙</button>
        </div>
    </div>

    <h1><%= pageTitle %></h1>

    <p style="margin-bottom: 20px;">
        <a href="/brainstorm" class="btn btn-primary" style="background-color: #8e44ad;">
            ✨ Générateur d'Idées IA
        </a>
    </p>

    <hr>
    
    <section>
        <h2>Vue d'Ensemble des Idées & Performances</h2>

        <div class="top-dashboard-layout">
            
            <div class="chart-container">
                <h3>Distribution des Idées (<%= totalIdeas %>)</h3>
                <canvas id="statusChart"></canvas>
            </div>

            <div class="right-kpi-container">
                <h3>🚀 Vos Statistiques Clés (28 Jours)</h3>
                
                <% 
                    // NOTE: Ces variables sont simulées avec les données de vos images.
                    // Elles doivent être envoyées depuis votre route /global-dashboard
                    const views28Days = '21.0K';
                    const watchTime28Days = '62.7';
                    const subs28Days = '+22';
                    const subsCurrent = 67;
                    const subsGoal = 1000;
                    const watchTimeCurrent = 4.5;
                    const watchTimeGoal = 4000;
                %>
                
                <div class="stats-grid-small">
                    <div class="stat-card-small">
                        <h4>Vues</h4>
                        <p class="stat-value" style="color: var(--secondary-color);"><%= views28Days %></p>
                    </div>
                    <div class="stat-card-small">
                        <h4>Visionnage (H)</h4>
                        <p class="stat-value"><%= watchTime28Days %></p>
                    </div>
                    <div class="stat-card-small">
                        <h4>Abonnés</h4>
                        <p class="stat-value" style="color: #2ecc71;"><%= subs28Days %></p>
                    </div>
                </div>

                <h3>🎯 Objectifs Monétisation</h3>
                
                <div class="progress-container">
                    <p>Abonnés: <%= subsCurrent %> / <%= subsGoal %></p>
                    <% const subsPercent = (subsCurrent / subsGoal) * 100; %>
                    <div class="progress-bar-bg">
                        <div class="progress-bar" style="width: <%= subsPercent.toFixed(2) %>%;"></div>
                    </div>
                </div>

                <div class="progress-container">
                    <p>Heures de Visionnage: <%= watchTimeCurrent %>h / <%= watchTimeGoal %>h</p>
                    <% const watchPercent = (watchTimeCurrent / watchTimeGoal) * 100; %>
                    <div class="progress-bar-bg">
                        <div class="progress-bar" style="width: <%= watchPercent.toFixed(2) %>%;"></div>
                    </div>
                </div>

                <a href="/" class="btn btn-primary" style="margin-top: 15px;">Voir la Liste d'Idées</a>
            </div>

        </div>

        <hr>
        
        <h2 style="margin-top: 30px;">Statuts Détaillés</h2>
        <div class="stats-grid">
            
            <div class="stat-card">
                <h3>Total des Idées</h3>
                <p class="status-default"><%= totalIdeas %></p>
            </div>

            <% Object.keys(statusCounts).forEach(status => { 
                const statusClass = 'status-' + status.replace(/\s/g, ''); 
            %>
                <div class="stat-card">
                    <h3><%= status %></h3>
                    <p class="<%= statusClass %>">
                        <%= statusCounts[status] %>
                    </p>
                </div>
            <% }); %>

        </div>
    </section>

    <hr>
    
    <section>
        <h2>Analyse des Créateurs Suivis</h2>

        <% if (allChannelStats && allChannelStats.length > 0) { %>
            <div class="stats-grid">
                <% allChannelStats.forEach(stats => { %>
                    <div class="stat-card" style="border: 2px solid var(--secondary-color);">
                        <div style="display: flex; align-items: center; margin-bottom: 10px; justify-content: center;">
                            <% if(stats.thumbnail) { %>
                                <img src="<%= stats.thumbnail %>" alt="<%= stats.title %>" style="width: 40px; height: 40px; border-radius: 50%; margin-right: 10px;">
                            <% } %>
                            <h3 style="font-size: 1.2em; color: var(--secondary-color); margin: 0;">
                                <%= stats.title || 'Chaîne Inconnue' %>
                            </h3>
                        </div>
                        <p style="font-size: 1.2em; color: var(--secondary-color); margin: 5px 0 0;">
                            Abonnés : 
                            <span style="font-weight: bold; color: var(--text-color);">
                                <%= (stats.subscriberCount || 0).toLocaleString('fr-FR') %>
                            </span>
                        </p>
                        <p style="font-size: 0.9em; color: #7f8c8d;">
                            Vues Totales : <%= (stats.viewCount || 0).toLocaleString('fr-FR') %>
                        </p>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p style="color: #7f8c8d;">
                Ajoutez des idées avec un nom de chaîne YouTube pour commencer à suivre leurs statistiques ici.
            </p>
        <% } %>
    </section>

    <hr>
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // --- GESTION DU GRAPHIQUE ---
            const statusData = <%- JSON.stringify(statusCounts) %>;
            const labels = Object.keys(statusData);
            const data = Object.values(statusData);

            const backgroundColors = labels.map(label => {
                switch(label) {
                    case 'Draft': return '#f1c40f'; 
                    case 'In Progress': return '#3498db'; 
                    case 'Published': return '#2ecc71'; 
                    case 'Archived': return '#95a5a6'; 
                    default: return '#2c3e50';
                }
            });

            const ctx = document.getElementById('statusChart');

            if (ctx) {
                new Chart(ctx, {
                    type: 'doughnut', 
                    data: {
                        labels: labels,
                        datasets: [{
                            label: 'Nombre d\'Idées',
                            data: data,
                            backgroundColor: backgroundColors,
                            hoverOffset: 4
                        }]
                    },
                    options: {
                        responsive: true,
                        plugins: {
                            legend: {
                                position: 'bottom', 
                            },
                            title: {
                                display: false,
                            }
                        }
                    }
                });
            }
            
            // --- GESTION DU MODE SOMBRE ---
            const toggle = document.getElementById('dark-mode-toggle');
            const body = document.body;
            const currentTheme = localStorage.getItem('theme');
            
            // Appliquer le thème sauvegardé au chargement
            if (currentTheme) {
                body.classList.remove('light-mode', 'dark-mode');
                body.classList.add(currentTheme);
                // Mise à jour de l'icône
                toggle.textContent = currentTheme === 'dark-mode' ? '☀️' : '🌙';
            } else {
                body.classList.add('light-mode');
                toggle.textContent = '🌙';
            }

            if(toggle) {
                toggle.addEventListener('click', () => {
                    // Déterminer le nouveau thème
                    const newTheme = body.classList.contains('light-mode') ? 'dark-mode' : 'light-mode';
                    
                    // Mettre à jour les classes et le localStorage
                    body.classList.remove('light-mode', 'dark-mode');
                    body.classList.add(newTheme);
                    localStorage.setItem('theme', newTheme);
                    
                    // Mettre à jour l'icône
                    toggle.textContent = newTheme === 'dark-mode' ? '☀️' : '🌙';
                });
            }
        });
    </script>
</body>
</html>