<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1><%= pageTitle %></h1>

    <section>
        <h2>Ajouter une Nouvelle Idée</h2>
        <form action="/idea" method="POST">
            <div>
                <label for="title">Titre de l'Idée :</label>
                <input type="text" id="title" name="title" required>
            </div>
            
            <div>
                <label for="description">Description :</label>
                <textarea id="description" name="description" required></textarea>
            </div>

            <div>
                <label for="category">Catégorie (YT, Blog, etc.) :</label>
                <input type="text" id="category" name="category" value="YouTube">
            </div>

            <button type="submit">Enregistrer l'Idée</button>
        </form>
    </section>

    <hr>

    <section>
        <h2>Liste des Idées</h2>
        
        <% if (ideas.length === 0) { %>
            <p>Aucune idée trouvée pour l'instant. Ajoutez-en une pour commencer !</p>
        <% } else { %>
            <div class="ideas-grid">
                <% ideas.forEach(idea => { %>
                    <div class="idea-card">
                        <h3><%= idea.title %></h3>
                        <p>
                            <strong>Statut:</strong> 
                            <span class="status-badge status-<%= idea.status.replace(/ /g, '') %>">
                                <%= idea.status %>
                            </span>
                        </p>
                        <p><strong>Description:</strong> <%= idea.description %></p>
                        <p>
                            <strong>Catégorie:</strong> <%= idea.category || 'N/A' %> | 
                            <strong>Date:</strong> <%= new Date(idea.createdAt).toLocaleDateString('fr-FR') %>
                        </p>
                        
                        <div class="idea-actions">
                            <a href="/edit/<%= idea._id %>" class="btn btn-edit">
                                Modifier
                            </a>
                            
                            <form action="/idea/delete/<%= idea._id %>" method="POST" style="display:inline;">
                                <button type="submit" class="btn btn-delete" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cette idée ?');">
                                    Supprimer
                                </button>
                            </form>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </section>

</body>
</html>